- Запуск контейнера Ollama

```bash
docker run -d --name ollama -p 11434:11434 ollama/ollama:latest
```
-d — запуск в фоне
--name ollama — чтобы контейнер назывался ollama
-p 11434:11434 — проброс порта для API

- Загрузка (внедрение) модели
  После запуска контейнера кидаем команду в сам контейнер или через exec, например, чтобы скачать Llama3:

```bash
docker exec ollama ollama pull llama3
```
  Или другую модель (например, Мistral):
```bash
docker exec ollama ollama pull mistral
```

- Запуск модели (чатить с моделью через терминал)
```bash
docker exec -it ollama ollama run llama3
```
- Использование Ollama API через curl (fetch in js)
  Для автоматизации:
```bash
curl http://localhost:11434/api/generate -d '{
  "model": "llama3",
  "prompt": "Ваш вопрос."
  }'
```

Частый процесс:

Стартуем контейнер
```bash
docker run -d --name ollama -p 11434:11434 ollama/ollama:latest
```

Загружаем модель
```bash
docker exec ollama ollama pull llama3
```

Запускаем модель
```bash
docker exec -it ollama ollama run llama3
```
💡 Если нужно запустить другую модель — вместо llama3 укажи нужную (например, phi3, gemma, mistral).
⚡️ Для автоматизации пиши свои вопросы через curl или инструменты, которые умеют слать запросы по API.
